services:
  - type: web
    name: offensive-cad-system
    runtime: python
    plan: free
    buildCommand: pip install -e .
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
